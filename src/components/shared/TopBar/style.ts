import { css } from '@emotion/react';

export const topBar = css`
  height: 32px;
  background-color: var(--primary);
  color: var(--white);
  display: grid;
  place-items: center;
  font-size: 16px;
  font-weight: bold;
`;
